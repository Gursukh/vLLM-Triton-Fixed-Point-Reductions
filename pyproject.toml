[project]
name = "det-vllm"
version = "0.1.0"
description = "Deterministic inference for vLLM via fixed-point reductions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vllm",
    "triton",
    "torch",
]

[project.entry-points."vllm.general_plugins"]
fixed_point_reductions = "vllm_fixed_point_reductions.register:register"

[project.optional-dependencies]
dev = [
    "pytest",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = [
    "vllm_fixed_point_reductions",
    "vllm_fixed_point_reductions.fixed_point_kernels",
    "vllm_fixed_point_reductions.vllm_modules",
]
